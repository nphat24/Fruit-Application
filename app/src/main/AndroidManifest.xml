<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fruit_application">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.ApplicationDelegate"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FruitApplication.NoActionBar">
        <activity android:name=".view.activity.YourOrderActivity"></activity>
        <activity android:name=".view.activity.AccountActivity" />
        <activity android:name=".view.activity.OrderPlacedActivity" />
        <activity android:name=".view.activity.AddCardActivity" />
        <activity
            android:name=".view.activity.DetailActivity"
            android:theme="@style/Theme.FruitApplication.IntroSlider" />
        <activity android:name=".view.activity.ShowMoreFruitActivity" />
        <activity android:name=".view.activity.WelcomeActivity" />
        <activity
            android:name=".view.activity.MainActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".view.activity.RegisterActivity" />
        <activity android:name=".view.activity.LoginActivity" />
        <activity
            android:name=".view.activity.SplashActivity"
            android:theme="@style/Theme.FruitApplication.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>